<UserControl x:Class="Kropka.EasyXaml.Client.Views.Views.SingleFileConverterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Kropka.EasyXaml.Client.Views.Resources.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Kropka.EasyXaml.Client.Views.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding CurrentConverterItem.SourcePath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Width="190"
                    Height="30"
                    Command="{Binding PickFileCommand}"
                    Content="Select" />
            <controls:ScriptEditor Width="600"
                                   Height="400"
                                   Margin="12"
                                   Padding="0,8,8,8"
                                   BorderBrush="#DADFE0"
                                   BorderThickness="1"
                                   FontFamily="Consolas"
                                   FontSize="14"
                                   HorizontalScrollBarVisibility="Auto"
                                   IsReadOnly="True"
                                   Script="{Binding CurrentConverterItem.ResultContent, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                   ShowLineNumbers="True"
                                   SyntaxHighlighting="XML"
                                   VerticalScrollBarVisibility="Auto"
                                   WordWrap="True">
                <controls:ScriptEditor.Resources>
                    <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="White" />
                        <Setter Property="BorderBrush" Value="White" />
                    </Style>
                </controls:ScriptEditor.Resources>
            </controls:ScriptEditor>
            <Button Width="190"
                    Height="30"
                    Command="{Binding CopyContentCommand}"
                    Content="Copy" />
            <Button Width="190"
                    Height="30"
                    Command="{Binding SaveFileCommand}"
                    Content="Save" />
        </StackPanel>
    </Grid>
</UserControl>
